daq:
  lcls: 1
  platform: 4

happi:
  beamline:
    - FEE
    - HXD
    - MFX

lightpath: MFX

load:
  - hutch.py

experiment:
  number: automatic
  import: experiment.User() as x
  features:
    - pswww

namespace:
  class:
    function:
      - s
      - scripts
    ophyd.PositionerBase:
      - m
      - motors
    pcdsdevices.PIM:
      - p
      - pims
    pcdsdevices.Slits:
      - s
      - slits
  happi:
    lcls:
      - beamline
      - stand
