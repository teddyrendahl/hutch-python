hutch: mfx

daq:
  lcls: 1
  platform: 4

happi:
  beamline:
    - FEE
    - HXD
    - MFX

lightpath: MFX

load:
  - hutch.py

experiment:
  #number: automatic
  number: mfxx000
  import: experiment.User() as x
  #features:
  #  - pswww

namespace:
  class:
    function:
      - s
      - scripts
    ophyd.PositionerBase:
      - m
      - motors
    pcdsdevices.PIM:
      - p
      - pims
    pcdsdevices.Slits:
      - s
      - slits
    ophyd.sim.SynSignal:
      - f
      - fake
    ophyd.sim.SynAxis:
      - f
      - fake
  happi:
    lcls:
      - beamline
      - stand
